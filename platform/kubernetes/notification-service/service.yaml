apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: nexora

spec:
  type: ClusterIP

  selector:
    app: notification-service

  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
