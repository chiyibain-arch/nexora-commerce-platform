apiVersion: v1
kind: Service

metadata:
  name: rating-service
  namespace: nexora

spec:
  type: ClusterIP

  selector:
    app: rating-service

  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
